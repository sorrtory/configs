# general {
#     lock_cmd = notify-send "lock!"          # dbus/sysd lock command (loginctl lock-session)
#     unlock_cmd = notify-send "unlock!"      # same as above, but unlock
#     before_sleep_cmd = notify-send "Zzz"    # command ran before sleep
#     after_sleep_cmd = notify-send "Awake!"  # command ran after sleep
#     ignore_dbus_inhibit = false             # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
#     ignore_systemd_inhibit = false          # whether to ignore systemd-inhibit --what=idle inhibitors
# }
# 
# listener {
#     timeout = 5                            # in seconds
#     on-timeout = notify-send "You are idle!" # command to run when timeout has passed
#     on-resume = notify-send "Welcome back!"  # command to run when activity is detected after timeout has fired.
# }

# Play MPV video after 1 minute of inactivity
#listener {
#timeout = 5 # 1 minute
#on-timeout = mpv --no-input-default-bindings --no-osd-bar --loop --quiet --no-audio --wid=$(hyprctl activewindow -j | jq -r '.address') ~/Pictures/Wallpapers/musashi.mp4

# Stop MPV (killall mpv) when activity resumes
#on-resume = pkill mpv
#}
# Optional: Lock screen after 10 minutes
#timeout = 600
#command = hyprlock


general {
    lock_cmd = /home/z/Documents/scripts/hypr_lockscreen.sh
}

listener {
    timeout = 5             # 5 minutes
    on-timeout = $lock
    on-resume = echo "Welcome back!"
}
